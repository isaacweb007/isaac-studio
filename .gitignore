# ── 이 저장소는 isaacstudio.cc 로 배포된다. 추적되는 것만 웹에 올라간다.
#    배포되는 것: index.html · CNAME · favicon 계열 · og.jpg · README.md
#                 previews/ · reels/ · refs/
#
#    그 외 제작 자료는 전부 _작업파일/ 안에 있고 배포되지 않는다.
#    ⚠ 저장소가 공개이므로, 원가·재무·미공개 소재는 반드시 _작업파일/ 안에 둔다.

_작업파일/

# 원본 마스터는 용량이 커서 올리지 않는다 (배포용 압축본은 reels/ refs/ previews/)
*.mov
*.mp4
!previews/*.mp4
!reels/*.mp4
!refs/*.mp4

# 편집 중 사본·백업
*.bak
랜딩페이지.html

.DS_Store
.claude/
